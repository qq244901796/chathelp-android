# 变更记录

## 1.4.3-glm

- 修复 GLM 返回完整有效概率表但 choice 使用未定义标签时，七项判断被整体拒绝的问题：最高项唯一时按概率表确定选项，不补造分数；概率并列且无有效选择时仍拒绝。
- 智谱判断提示改为完整的有效 JSON 示例，明确要求使用规定的英文选项键。
- 判断、回复和排序遇到结构校验错误时最多纠正重生成一次；传输、密钥和内容过滤错误不触发格式重生成。
- 错误提示区分判断、回复和排序，并显示具体字段规则；不显示模型原文或密钥。
- 使用测试机已配置的默认免费智谱模型，对 Demo 两组虚构对话实际完成七项判断、三条回复和排序。新增对应回归测试；联网测试需显式启用，默认跳过。
- versionCode 8；同签名覆盖升级。

## 1.4.2-glm

- 新增独立「ChatHelp 测试聊天」1.0.0 APK，无需登录，支持两组虚构会话、模拟双方消息、重置与清空。
- 增加测试软件适配器：普通文字采集、Canvas 气泡真实 OCR、候选回复填入。
- 手动分析在未识别到会话、空会话、OCR 关闭或任务进行中时给出反馈。
- 气泡 OCR 按当前适配器刷新区域；测试页面的模式切换、重置及内容代次参与结果失效判定。
- 测试软件不申请网络权限；智谱仍由 ChatHelp 使用用户配置的密钥调用。
- versionCode 7，沿用发布签名和应用 ID，可覆盖升级 1.4.1-glm。

## 1.4.1-glm

- 基于 Jev Android，默认智谱 GLM-4-Flash-250414 完成判断、回复、排序。
- 保留本机 ML Kit 中文 OCR，可关闭；默认关闭独立云视觉测试。
- 严格检查结构化输出，串行请求，限制重试，隔离供应商密钥，丢弃过期结果。
- 增加使用虚构对话的设置测试；保留旧配置，不强制迁移。
- 更名 ChatHelp 智谱助手，新增原创图标、离线关于/隐私/许可，保留原作者与历史。
- versionCode 6；沿用 1.4-glm 发布包的应用 ID 和签名，可覆盖升级。
- 原版安装包及宣传入口不再出现在当前分支；安装包改为 Release 附件。
