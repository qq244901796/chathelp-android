<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Intentionally no permissions: messages exist only in this simulation. -->
    <application android:allowBackup="false" android:label="ChatHelp 测试聊天"
        android:icon="@drawable/ic_test_chat" android:theme="@style/TestChatTheme">
        <activity android:name=".MainActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
