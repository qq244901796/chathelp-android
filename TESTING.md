# 测试范围

测试数据均为虚构文字与合成中文图片；不读取实际联系人或聊天，不要求把 API Key 交给维护者。

自动化覆盖：JSON 输出、概率/枚举校验、本地 HTTP 请求、限流/失败重试、串行和过期会话；Android 配置迁移、供应商密钥隔离、禁用云视觉、真实 ML Kit 中文 OCR。

1.4.1-glm 实测：16 项 JVM 单测通过；华为 NOH-AN00、Android 12（API 31）、ARM64 上 7 项 release 设备测试通过，包括完整许可页异步加载与翻页。另关闭 Wi-Fi/移动数据后，合成中文 OCR 测试通过；测试后恢复网络并移除辅助测试包。该设备启用了 GMS，不能据此宣称完成无 GMS 验证。

新版已从 1.4-glm 同签名覆盖安装成功。APK 的 v2 签名及 16 KB 原生库对齐检查通过；证书 SHA256：`ab44949edda3a80441755da02f88c99a7431dee9e5d5fa2b046ff5361419ce02`。Windows 产品的打包源码和验证在独立仓库中维护。

尚未验证：真实智谱密钥的在线端到端响应质量；真实微信/QQ 会话中的连续采集和填入；未安装 GMS 的手机；不同厂商后台策略。完整流程按钮可在你填写自己的 Key 后验证在线接口。

本分支不把上游 docs/v1.3-* 中的原机型结论当作本机复测结果。每个 Release 的说明列出对应包实际完成的测试与文件校验和。
