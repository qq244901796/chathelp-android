# 测试范围

测试数据均为虚构文字与合成中文图片；不读取实际联系人或聊天，不要求把 API Key 交给维护者。

## 1.4.3-glm

真机复现 1.4.2 判断失败：返回的 `true_intent.choice` 不属于规定选项，但该项概率表包含全部 6 个选项且总和为 1。使用的默认模型为 `glm-4-flash-250414`。

修复后 22 项 JVM 单测、12 项非联网设备回归测试通过，覆盖有效概率表推导选项、并列处理、拒绝缺失/越界数据、一次格式重生成及失败边界。另显式启用联网设备测试，Demo 的图书馆、周末公园两组虚构对话都通过真实智谱判断、三条回复和排序。只使用手机应用内已配置的 Key，未导出密钥或记录原始响应。

`LiveBigModelInstrumentedTest` 默认跳过，不在普通回归中访问外网。手动执行时仅接受智谱官方域名和指定免费模型，使用 `-e liveBigModel verify`。`diagnose` 模式仅输出字段类型、枚举是否合法及概率统计，不输出原始选项标签、回复或聊天内容。测试未覆盖真实聊天质量、所有对话或所有手机厂商。

## 1.4.2-glm 与测试聊天 1.0.0

在华为 NOH-AN00、Android 12（API 31）、ARM64 上运行 release 设备测试，12 项通过；现有 16 项 JVM 单测通过。两只签名 APK 均成功安装，ChatHelp 覆盖升级保留原配置。

新增 5 项设备测试：读取测试聊天的真实无障碍节点并区分双方、排除输入草稿和工具按钮；Canvas 模式正文不可直接读取，真实屏幕截图经 ML Kit 识别两个气泡；重置/切换模式/切换会话使旧代次失效，空页面不触发 OCR；调用生产采集逻辑验证来信触发及手动分析；调用生产填入逻辑确认仅修改输入框、不追加消息，且拒绝切换会话以及 A→B→A 后的旧回复。

测试通过 UiAutomation 和隔离配置运行，没有启用真实无障碍采集服务，没有修改用户密钥。测试服务使用真实界面作为输入，模型密钥为空以阻止网络请求。这覆盖适配器、截图识别、触发与填入逻辑，不等于已经验证真实智谱在线响应或各手机厂商的后台服务行为。

测试步骤见 [测试聊天说明](test-chat/README.md)。设备测试需先安装测试聊天 APK。通用整屏「截屏识别一次」仍不能可靠区分说话方。

## 历史验证

自动化覆盖：JSON 输出、概率/枚举校验、本地 HTTP 请求、限流/失败重试、串行和过期会话；Android 配置迁移、供应商密钥隔离、禁用云视觉、真实 ML Kit 中文 OCR。

1.4.1-glm 实测：16 项 JVM 单测通过；华为 NOH-AN00、Android 12（API 31）、ARM64 上 7 项 release 设备测试通过，包括完整许可页异步加载与翻页。另关闭 Wi-Fi/移动数据后，合成中文 OCR 测试通过；测试后恢复网络并移除辅助测试包。该设备启用了 GMS，不能据此宣称完成无 GMS 验证。

新版已从 1.4-glm 同签名覆盖安装成功。APK 的 v2 签名及 16 KB 原生库对齐检查通过；证书 SHA256：`ab44949edda3a80441755da02f88c99a7431dee9e5d5fa2b046ff5361419ce02`。Windows 产品的打包源码和验证在独立仓库中维护。

尚未验证：真实智谱密钥的在线端到端响应质量；真实微信/QQ 会话中的连续采集和填入；未安装 GMS 的手机；不同厂商后台策略。完整流程按钮可在你填写自己的 Key 后验证在线接口。

本分支不把上游 docs/v1.3-* 中的原机型结论当作本机复测结果。每个 Release 的说明列出对应包实际完成的测试与文件校验和。
